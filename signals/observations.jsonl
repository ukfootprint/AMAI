{"_example": true, "date": "YYYY-MM-DD", "context": "One line: what the session was about", "signals": ["Override: rejected suggestion to X because it felt like Y", "Friction: AI kept framing Z in a way that required editing every time", "Positive: output on [topic] felt unusually on-brand — worth understanding why", "Pattern: third session this week where I've noticed [behaviour]"], "possible_divergence": "Optional — e.g. AI keeps suggesting discounting as a growth tactic", "config_ref": "Optional — e.g. identity/heuristics.yaml → no_discount_growth"}
